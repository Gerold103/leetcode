There is a set of tasks having dependencies on each other. Make a function which
takes the tasks in any form and prints an order in which the tasks can be
executed without violating dependencies.

If it is not possible, need to return an error.
